public with sharing class calculadora {
   // calculadora que soma e subtrai
    public static Integer soma(Integer a, Integer b) {
        return a + b;
    }

    public static Integer subtrai(Integer a, Integer b) {
        return a - b;
    }
}